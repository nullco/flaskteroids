[project]
name = "flaskteroids"
version = "0.1.0"
description = "Flask on steroids"
readme = "README.md"
dependencies = [
	"alembic>=1.15.1",
	"Flask",
	"sqlalchemy",
]


[tool.flake8]
max-line-length = 120

[dependency-groups]
dev = [
    "pytest>=8.3.5",
    "ruff>=0.10.0",
]
