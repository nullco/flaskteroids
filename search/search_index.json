{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Intro","text":"<p>Flaskteroids (or Flask on Steroids) is a lightweight yet powerful Python MVC framework designed to provide the closest possible experience to developing web applications in Ruby on Rails, in the python world. By supercharging Flask with Rails-inspired conventions and structure, Flaskteroids enables developers to build full-featured web applications with unprecedented speed and elegance.</p> <p>Our mission is to bring the proven productivity and developer happiness of Rails to the Python ecosystem, offering a batteries-included framework that emphasizes convention over configuration. This means you can focus on building great applications rather than boilerplate code, while maintaining the full power of Flask and its rich ecosystem.</p> <p>Flaskteroids stays lean, fast, and secure by carefully selecting core dependencies and providing everything you need out of the box \u2014 from database management to background jobs \u2014 without unnecessary complexity.</p>"},{"location":"#features","title":"Features","text":"<ul> <li>Full MVC Architecture: Clean separation of concerns with Models, Views, and Controllers, just like Rails.</li> <li>Elegant Routing: Intuitive and resourceful routing that automatically generates RESTful routes for your resources.</li> <li>Built-in ORM: Seamless integration with SQLAlchemy and Alembic for database management and migrations.</li> <li>Background Jobs: Integrated with Celery for easy background job processing.</li> <li>RESTful by Design: Quickly build API routes with automatic JSON responses for data-oriented actions.</li> <li>Powerful CLI: A rich set of commands for generating models, controllers, mailers, scaffolds, and more \u2014 inspired by Rails generators.</li> <li>Flask Compatibility: Retains the full power of Flask and its rich ecosystem while adding Rails-like productivity.</li> <li>Convention Over Configuration: Sensible defaults and automatic discovery reduce boilerplate code.</li> </ul>"},{"location":"getting_started/","title":"Getting started","text":""},{"location":"getting_started/#installation","title":"Installation","text":"<pre><code>pip install flaskteroids\n</code></pre>"},{"location":"getting_started/#creating-a-new-app","title":"Creating a New App","text":"<p>Create a new Flaskteroids application using the <code>flaskteroids new</code> command:</p> <pre><code>flaskteroids new my_app\ncd my_app\n</code></pre> <p>This creates a new directory called <code>my_app</code> with a standard Rails-inspired application structure.</p>"},{"location":"getting_started/#project-structure","title":"Project Structure","text":"<p>A typical Flaskteroids application follows this structure:</p> <pre><code>my_app/\n\u251c\u2500\u2500 app/\n\u2502   \u251c\u2500\u2500 controllers/     # Controllers handle requests and responses\n\u2502   \u251c\u2500\u2500 models/          # Models represent database tables and business logic\n\u2502   \u251c\u2500\u2500 views/           # Templates for rendering HTML\n\u2502   \u2514\u2500\u2500 mailers/         # Email templates and logic\n\u251c\u2500\u2500 config/\n\u2502   \u251c\u2500\u2500 routes.py        # Route definitions\n\u251c\u2500\u2500 db/\n\u2502   \u2514\u2500\u2500 migrations/      # Database migration files\n\u251c\u2500\u2500 public/              # Static assets (CSS, JS, images)\n\u251c\u2500\u2500 tests/               # Test files\n\u251c\u2500\u2500 pyproject.toml       # Project configuration\n\u2514\u2500\u2500 wsgi.py               # Main application entry point\n</code></pre>"},{"location":"getting_started/#running-the-server","title":"Running the Server","text":"<p>To start the development server, run:</p> <pre><code>flask run\n</code></pre> <p>Now, open your browser and navigate to <code>http://127.0.0.1:5000</code>. You should see the Flaskteroids welcome page!</p>"},{"location":"getting_started/#your-first-feature-a-blog","title":"Your First Feature: A Blog","text":"<p>Let's create a simple blog to see the power of scaffolding. Inside your <code>my_app</code> follow the next steps:</p> <ol> <li>Generate a Post scaffold:    This command will create the model, controller, views, and database    migration for a <code>Post</code> resource with <code>title</code> and <code>content</code> fields.</li> </ol> <pre><code>flask generate scaffold Post title:string content:text\n</code></pre> <ol> <li>Run the database migration:    Apply the changes to your database schema.</li> </ol> <pre><code>flask db:migrate\n</code></pre> <ol> <li>Start the server:</li> </ol> <pre><code>flask run\n</code></pre> <p>Now, visit <code>http://127.0.0.1:5000/posts</code> in your browser. You have a complete set of pages to create, view, update, and delete posts.</p>"},{"location":"getting_started/#next-steps","title":"Next Steps","text":"<p>Congratulations on creating your first Flaskteroids application! Here are some suggestions for what to explore next:</p> <ul> <li>Learn the Basics: Dive deeper into Models, Views and Controllers.</li> </ul>"},{"location":"project_structure/","title":"Project Structure","text":"<p>A typical Flaskteroids project follows this structure:</p> <pre><code>my_project/\n\u251c\u2500\u2500 app/\n\u2502   \u251c\u2500\u2500 controllers/\n\u2502   \u251c\u2500\u2500 models/\n\u2502   \u251c\u2500\u2500 mailers/\n\u2502   \u251c\u2500\u2500 views/\n\u2502   \u2514\u2500\u2500 jobs/\n\u251c\u2500\u2500 config/\n\u2502   \u2514\u2500\u2500 routes.py\n\u2514\u2500\u2500 run.py\n</code></pre> <ul> <li><code>app/controllers</code>: Handle web requests and respond with data or rendered views.</li> <li><code>app/models</code>: Define your application's data structure and database interactions.</li> <li><code>app/views</code>: Contain the templates for your application's UI.</li> <li><code>app/mailers</code>: Handle sending emails.</li> <li><code>app/jobs</code>: Define background jobs to be run asynchronously.</li> <li><code>config/routes.py</code>: Define the URL routes for your application.</li> </ul>"},{"location":"controllers/","title":"Controllers","text":"<p>Controllers handle the logic for incoming requests. Actions are methods within a controller class that set instance variables for the view. Rendering is handled automatically.</p> <pre><code># app/controllers/posts_controller.py\nfrom flaskteroids import params\nfrom flaskteroids.controller import ActionController\nfrom app.models.post import Post\n\nclass PostsController(ActionController):\n    def index(self):\n        self.posts = Post.all()\n        # Implicitly renders app/views/posts/index.html and\n        # makes self.posts available in the template.\n\n    def show(self):\n        self.post = Post.find(params[\"id\"])\n        # Implicitly renders app/views/posts/show.html and\n        # makes self.post available in the template.\n</code></pre>"},{"location":"controllers/#controller-callbacks","title":"Controller Callbacks","text":"<p>You can use callbacks to run code before a controller action. The <code>before_action</code> callback is useful for setting up instance variables or performing authentication checks. Callbacks must be defined within a <code>@rules</code> decorator.</p> <pre><code>from flaskteroids import params\nfrom flaskteroids.actions import before_action\nfrom flaskteroids.rules import rules\nfrom flaskteroids.controller import ActionController\nfrom app.models.post import Post\n\n@rules(\n    before_action('_set_post', only=['show', 'edit', 'update', 'destroy'])\n)\nclass PostsController(ActionController):\n    def show(self):\n        # self.post is already set by the callback\n        pass\n\n    def _set_post(self):\n        self.post = Post.find(params[\"id\"])\n</code></pre>"},{"location":"controllers/#content-negotiation","title":"Content Negotiation","text":"<p>Flaskteroids can handle different response formats within a single action using the <code>respond</code> method. This is particularly useful for building APIs that serve both HTML and JSON.</p> <pre><code>from flaskteroids.controller import ActionController, respond\nfrom app.models.post import Post\n\nclass PostsController(ActionController):\n    def index(self):\n        self.posts = Post.all()\n        return respond(\n            html=lambda: render('index'),\n            json=lambda: render(json=self.posts)\n        )\n</code></pre>"},{"location":"controllers/advanced/","title":"Rate Limit","text":"<p>You can use the rate_limit rule to restrict how often a particular controller or action can be called within a given time window.</p> <pre><code>from flaskteroids.controller import ActionController \nfrom flaskteroids.rules import rules\nfrom flaskteroids.rate_limit import rate_limit\n\n\n@rules(\n    rate_limit(to=5, within=60),\n)\nclass MyController(ActionController):\n\n    def index(self):\n        pass\n</code></pre>"},{"location":"controllers/routing/","title":"Routing","text":"<p>Flaskteroids provides a powerful routing system that allows you to define routes in a clean, RESTful manner. Routes are defined in <code>config/routes.py</code> by a <code>register</code> function that receives a router instance. You can define standard routes or use resourceful routing to handle RESTful conventions automatically.</p>"},{"location":"controllers/routing/#route-format","title":"Route Format","text":"<p>Routes map HTTP requests to controller actions using the format <code>\"controller#action\"</code>. Controllers are automatically discovered from the <code>CONTROLLERS['LOCATION']</code> directory (default: <code>app/controllers</code>), and their class names are derived by camelizing the controller name and appending \"Controller\" (e.g., <code>posts</code> becomes <code>PostsController</code>).</p>"},{"location":"controllers/routing/#standard-routes","title":"Standard Routes","text":"<p>Standard routes allow you to manually define routes for specific HTTP methods and paths.</p>"},{"location":"controllers/routing/#get-routes","title":"GET Routes","text":"<p>Define a GET route using <code>router.get(path, to=\"controller#action\", as_=None)</code>:</p> <pre><code># config/routes.py\ndef register(router):\n    router.get(\"/\", to=\"welcome#index\")\n    router.get(\"/about\", to=\"pages#about\")\n    router.get(\"/users/&lt;int:id&gt;\", to=\"users#show\")\n</code></pre>"},{"location":"controllers/routing/#post-routes","title":"POST Routes","text":"<p>Define a POST route using <code>router.post(path, to=\"controller#action\", as_=None)</code>:</p> <pre><code>def register(router):\n    router.post(\"/users\", to=\"users#create\")\n    router.post(\"/login\", to=\"sessions#create\")\n</code></pre>"},{"location":"controllers/routing/#put-routes","title":"PUT Routes","text":"<p>Define a PUT route using <code>router.put(path, to=\"controller#action\", as_=None)</code>:</p> <pre><code>def register(router):\n    router.put(\"/users/&lt;int:id&gt;\", to=\"users#update\")\n</code></pre>"},{"location":"controllers/routing/#delete-routes","title":"DELETE Routes","text":"<p>Define a DELETE route using <code>router.delete(path, to=\"controller#action\", as_=None)</code>:</p> <pre><code>def register(router):\n    router.delete(\"/users/&lt;int:id&gt;\", to=\"users#destroy\")\n</code></pre>"},{"location":"controllers/routing/#root-route","title":"Root Route","text":"<p>The root route maps the home page. If no root route is defined, Flaskteroids automatically adds one pointing to the internal welcome controller.</p> <pre><code>def register(router):\n    router.root(to=\"welcome#index\")  # or router.get(\"/\", to=\"welcome#index\")\n</code></pre>"},{"location":"controllers/routing/#resourceful-routes","title":"Resourceful Routes","text":"<p>Resourceful routing provides automatic generation of RESTful routes for a controller. This follows Rails conventions and eliminates repetitive route definitions.</p>"},{"location":"controllers/routing/#resources-plural","title":"Resources (Plural)","text":"<p>The <code>router.resources(name, param=None, only=None, nested=None)</code> method generates standard CRUD routes for a plural resource:</p> <pre><code>def register(router):\n    router.resources(\"posts\")\n</code></pre> <p>This generates the following routes:</p> HTTP Method Path Controller#Action Named Route GET /posts/ posts#index index_post GET /posts/new/ posts#new new_post POST /posts/ posts#create create_post GET /posts// posts#show show_post GET /posts//edit/ posts#edit edit_post PUT /posts// posts#update update_post DELETE /posts// posts#destroy destroy_post <p>By default, the <code>:id</code> parameter is an integer, but you can customize it:</p> <pre><code>def register(router):\n    router.resources(\"posts\", param=\"int:post_id\")\n    # or for string IDs\n    router.resources(\"posts\", param=\"post_id\")\n</code></pre>"},{"location":"controllers/routing/#limiting-actions","title":"Limiting Actions","text":"<p>Use the <code>only</code> parameter to generate only specific actions:</p> <pre><code>def register(router):\n    router.resources(\"posts\", only=[\"index\", \"show\"])\n    # Generates only GET /posts/ and GET /posts/:id/\n</code></pre>"},{"location":"controllers/routing/#resource-singular","title":"Resource (Singular)","text":"<p>For singular resources (like <code>/profile</code>), use <code>router.resource(name, only=None)</code>:</p> <pre><code>def register(router):\n    router.resource(\"profile\")\n</code></pre> <p>This generates:</p> HTTP Method Path Controller#Action Named Route GET /profile/new/ profiles#new new_profile POST /profile/ profiles#create create_profile GET /profile/ profiles#show show_profile GET /profile/edit/ profiles#edit edit_profile PUT /profile/ profiles#update update_profile DELETE /profile/ profiles#destroy destroy_profile <p>Note: The controller name is pluralized (<code>profiles</code>), even though the resource is singular.</p>"},{"location":"controllers/routing/#nested-resources","title":"Nested Resources","text":"<p>Nest resources to create hierarchical routes:</p> <pre><code>def register(router):\n    router.resources(\"users\", nested=lambda r: [\n        *r.resources(\"posts\"),\n        *r.resources(\"comments\")\n    ])\n</code></pre> <p>This generates nested routes like:</p> <ul> <li>GET /users//posts/ <li>POST /users//posts/ <li>GET /users//posts// <li>etc.</li>"},{"location":"controllers/routing/#named-routes","title":"Named Routes","text":"<p>All routes automatically receive a generated name, but you can customize it with the <code>as_</code> parameter:</p> <pre><code>def register(router):\n    router.get(\"/dashboard\", to=\"users#dashboard\", as_=\"user_dashboard\")\n    router.resources(\"posts\", as_=\"articles\")  # Affects all generated routes\n</code></pre>"},{"location":"controllers/routing/#json-routes","title":"JSON Routes","text":"<p>Flaskteroids automatically adds JSON format routes for data-oriented actions:</p> <p>For resourceful routes, the following JSON routes are added:</p> <ul> <li>GET /posts/.json/ \u2192 posts#index</li> <li>POST /posts/.json/ \u2192 posts#create</li> <li>GET /posts//.json/ \u2192 posts#show <li>PUT /posts//.json/ \u2192 posts#update <li>DELETE /posts//.json/ \u2192 posts#destroy"},{"location":"controllers/routing/#method-overrides","title":"Method Overrides","text":"<p>Since browsers typically only support GET and POST, Flaskteroids provides method override support for PUT and DELETE requests. Include a <code>_method</code> parameter in POST requests:</p> <pre><code>&lt;form action=\"/posts/1\" method=\"POST\"&gt;\n  &lt;input type=\"hidden\" name=\"_method\" value=\"PUT\"&gt;\n  &lt;!-- form fields --&gt;\n&lt;/form&gt;\n</code></pre> <p>This allows the same URL to handle different HTTP methods based on the <code>_method</code> parameter.</p>"},{"location":"models/","title":"Models","text":"<p>Flaskteroids models provide a rich, Active Record-inspired API for working with your database. Models inherit from <code>flaskteroids.model.Model</code> and act as a wrapper around SQLAlchemy table objects. Database columns defined in migrations are automatically available as attributes on the model instances.</p> <p>Models support associations, validations, secure password handling, and provide a comprehensive querying interface.</p>"},{"location":"models/#defining-a-model","title":"Defining a Model","text":"<p>Models are defined by inheriting from <code>Model</code> and using the <code>@rules</code> decorator to apply associations, validations, and other features. The decorators (belongs_to, has_many, validates, etc.) are defined inside the rules decorator.</p> <pre><code># app/models/post.py\nfrom flaskteroids.model import Model, validates, belongs_to, has_many\nfrom flaskteroids.rules import rules\n\n@rules(\n    # --- Associations ---\n    belongs_to('user'),\n    has_many('comments'),\n\n    # --- Validations ---\n    validates('title', presence=True, length={'minimum': 5}),\n    validates('content', presence=True)\n)\nclass Post(Model):\n    # Database columns (title, content, user_id, etc.) are automatically available\n    pass\n</code></pre>"},{"location":"models/#associations","title":"Associations","text":"<p>Associations define relationships between models.</p>"},{"location":"models/#belongs_to","title":"belongs_to","text":"<p>Defines a one-to-one or many-to-one relationship where the model belongs to another model.</p> <pre><code>from flaskteroids.model import Model, belongs_to\nfrom flaskteroids.rules import rules\n\n@rules(\n    belongs_to('user')\n)\nclass Post(Model):\n    pass\n\n# Usage\npost = Post.find(1)\nuser = post.user  # Access the associated User\n</code></pre> <p>You can customize the association:</p> <pre><code>from flaskteroids.model import Model, belongs_to\nfrom flaskteroids.rules import rules\n\n\n@rules(\n    belongs_to('author', class_name='User', foreign_key='author_id')\n)\nclass Post(Model):\n    pass\n</code></pre>"},{"location":"models/#has_many","title":"has_many","text":"<p>Defines a one-to-many relationship where the model has many instances of another model.</p> <pre><code>from flaskteroids.model import Model, has_many\nfrom flaskteroids.rules import rules\n\n@rules(\n    has_many('comments')\n)\nclass Post(Model):\n    pass\n\n# Usage\npost = Post.find(1)\ncomments = post.comments  # Returns a Relation object\nfor comment in comments:\n    print(comment.content)\n\n# Create associated records\nnew_comment = post.comments.create(content=\"Great post!\", author=\"John\")\n</code></pre> <p>You can customize with <code>dependent</code> to handle cascading deletes:</p> <pre><code>from flaskteroids.model import Model, has_many\nfrom flaskteroids.rules import rules\n\n@rules(\n    has_many('comments', dependent='destroy')  # Deletes comments when post is deleted\n)\nclass Post(Model):\n    pass\n</code></pre>"},{"location":"models/#validations","title":"Validations","text":"<p>Validations ensure data integrity before saving records.</p>"},{"location":"models/#presence","title":"Presence","text":"<p>Ensures a field is not empty.</p> <pre><code>from flaskteroids.model import Model, validates\nfrom flaskteroids.rules import rules\n\n@rules(\n    validates('title', presence=True)\n)\nclass Post(Model):\n    pass\n</code></pre>"},{"location":"models/#length","title":"Length","text":"<p>Validates the length of a string field.</p> <pre><code>from flaskteroids.model import Model, validates\nfrom flaskteroids.rules import rules\n\n\n@rules(\n    validates('title', length={'minimum': 5, 'maximum': 100}),\n    validates('password', length={'minimum': 8})\n)\nclass Post(Model):\n    pass\n</code></pre>"},{"location":"models/#confirmation","title":"Confirmation","text":"<p>Validates that two fields match (commonly used for passwords).</p> <pre><code>from flaskteroids.model import Model, validates\nfrom flaskteroids.rules import rules\n\n\n@rules(\n    validates('password', confirmation=True)\n)\nclass User(Model):\n    pass\n\n# Requires both 'password' and 'password_confirmation' fields to match\nuser = User.new(password=\"secret\", password_confirmation=\"secret\")\n</code></pre>"},{"location":"models/#secure-passwords","title":"Secure Passwords","text":"<p>Adds secure password hashing and authentication methods.</p> <pre><code>from flaskteroids.model import Model, has_secure_password\n\n@rules(\n    has_secure_password()\n)\nclass User(Model):\n    pass\n\n# Usage\nuser = User.create(email=\"user@example.com\", password=\"secret\")\nuser.authenticate(\"secret\")  # Returns True\n\n# Class methods\nUser.authenticate_by(email=\"user@example.com\", password=\"secret\")  # Returns user or None\n</code></pre> <p>The decorator adds:</p> <ul> <li>Automatic password hashing to <code>password_digest</code></li> <li>Virtual fields: <code>password</code>, <code>password_confirmation</code>, <code>password_reset_token</code></li> <li>Validation for password length and confirmation</li> </ul>"},{"location":"models/#querying-models","title":"Querying Models","text":"<p>Models provide a rich querying interface through the <code>ModelQuery</code> class.</p>"},{"location":"models/#finding-records","title":"Finding Records","text":"<pre><code># Find by ID\npost = Post.find(1)\n\n# Find first by criteria\npost = Post.find_by(title=\"Hello World\")\n\n# Get all records\nposts = list(Post.all())\n</code></pre>"},{"location":"models/#where-clauses","title":"Where Clauses","text":"<pre><code># Simple where\nposts = Post.where(published=True)\n\n# Multiple conditions\nposts = Post.where(published=True).where(user_id=1)\n\n# Using expressions\nfrom sqlalchemy import or_\nposts = Post.where(or_(Post.title.like(\"%hello%\"), Post.content.like(\"%world%\")))\n</code></pre>"},{"location":"models/#includes-eager-loading","title":"Includes (Eager Loading)","text":"<p>Load associated records to avoid N+1 queries.</p> <pre><code># Eager load associations\nposts = Post.includes('user', 'comments').all()\n\nfor post in posts:\n    print(post.user.name)  # No additional query\n    for comment in post.comments:\n        print(comment.content)  # No additional query\n</code></pre>"},{"location":"models/#ordering","title":"Ordering","text":"<pre><code># Order by created_at descending\nposts = Post.order(created_at='desc').all()\n\n# Multiple order clauses\nposts = Post.order(created_at='desc', title='asc').all()\n</code></pre>"},{"location":"models/#crud-operations","title":"CRUD Operations","text":""},{"location":"models/#creating-records","title":"Creating Records","text":"<pre><code># Create and save immediately\npost = Post.create(title=\"New Post\", content=\"Content here\")\n\n# Create without saving\npost = Post.new(title=\"New Post\", content=\"Content here\")\npost.save()  # Returns True on success\n</code></pre>"},{"location":"models/#updating-records","title":"Updating Records","text":"<pre><code>post = Post.find(1)\npost.title = \"Updated Title\"\npost.save()\n\n# Or update multiple fields at once\npost.update(title=\"New Title\", content=\"New Content\")\n</code></pre>"},{"location":"models/#deleting-records","title":"Deleting Records","text":"<pre><code>post = Post.find(1)\npost.destroy()\n</code></pre>"},{"location":"models/#error-handling","title":"Error Handling","text":"<p>Models include an <code>errors</code> object for validation errors.</p> <pre><code>post = Post.new(title=\"\")  # Invalid: title is required\npost.save()  # Returns False\n\nif post.errors:\n    print(\"Errors:\", post.errors.full_messages())\n\n# Errors are automatically cleared on successful save\n</code></pre>"},{"location":"models/#authentication","title":"Authentication","text":"<p>For models with secure passwords:</p> <pre><code># Authenticate a user\nuser = User.authenticate_by(email=\"user@example.com\", password=\"secret\")\n\n# Password reset tokens\ntoken = user.password_reset_token\n# Later...\nuser = User.find_by_password_reset_token(token)\n</code></pre>"},{"location":"other/cli/","title":"Command-Line Interface (CLI)","text":"<p>Flaskteroids extends the <code>flask</code> CLI with a powerful set of tools to streamline development.</p>"},{"location":"other/cli/#generators","title":"Generators","text":"<p>Use the <code>flask generate</code> command to create various components:</p>"},{"location":"other/cli/#migration","title":"Migration","text":"<p>Allows generation of database migrations. You can specify different migration commands depending on what you want to achieve. All tables come by default with an auto-increment <code>id</code> field, and a <code>created_at</code> date field.</p>"},{"location":"other/cli/#creating-new-tables","title":"Creating new tables","text":"<pre><code>flask generate migration &lt;CreateXXXTable&gt; [field1:type ...]\n</code></pre> <p>Example: <code>flask generate migration CreateProducts name:string!</code></p>"},{"location":"other/cli/#adding-columns-to-existing-table","title":"Adding columns to existing table","text":"<pre><code>flask generate migration &lt;AddXXXToXXX&gt; [field1:type ...]\n</code></pre> <p>Example: <code>flask generate migration AddPriceToProducts price:float</code></p>"},{"location":"other/cli/#removing-columns-from-an-existing-table","title":"Removing columns from an existing table","text":"<pre><code>flask generate migration &lt;RemoveXXXFromXXX&gt; [field1 ...]\n</code></pre> <p>Example: <code>flask generate migration RemovePriceFromProducts price</code></p>"},{"location":"other/cli/#dropping-an-existing-table","title":"Dropping an existing table","text":"<pre><code>flask generate migration &lt;DropXXXX&gt; [field1 ...]\n</code></pre> <p>Example: <code>flask generate migration DropProducts</code></p>"},{"location":"other/cli/#model","title":"Model","text":"<p>This will created together a Model and it's corresponding migration. See Migrations for further details.</p> <pre><code>flask generate model &lt;ModelName&gt; [field1:type ...]\n</code></pre> <p>Example: <code>flask generate model User name:string email:string</code></p>"},{"location":"other/cli/#controller","title":"Controller","text":"<p>This will generate:</p> <ul> <li>A new controller with the actions specified.</li> <li>A views for each action.</li> <li>Entries in routes file linking all controller actions individually</li> </ul> <pre><code>flask generate controller &lt;ControllerName&gt; [action1 action2 ...]\n</code></pre> <p>Example: <code>flask generate controller Users index show</code></p>"},{"location":"other/cli/#resource","title":"Resource","text":"<p>This will Generate:</p> <ul> <li>A model and it's migration.</li> <li>An empty controller associated to that model.</li> <li>A resource entry linking the controller in routes.</li> </ul> <p>All the code is up to be written by you.</p> <pre><code>flask generate resource &lt;ResourceName&gt; [field1:type ...]\n</code></pre> <p>Generates a model and a RESTful controller.</p>"},{"location":"other/cli/#scaffold","title":"Scaffold","text":"<p>This will generate:</p> <ul> <li>A model and it's migration.</li> <li>A controller associated to that model fully supporting CRUD operations.   (The controller will have <code>index</code>, <code>show</code>, <code>new</code>, <code>edit</code>, <code>create</code>, <code>update</code>   and <code>destroy</code> actions)</li> <li>All views backing up CRUD operations</li> <li>A resource entry linking the controller in routes.</li> </ul> <pre><code>flask generate scaffold &lt;scaffold_name&gt; [field1:type ...]\n</code></pre> <p>Generates a model, RESTful controller, and views.</p>"},{"location":"other/cli/#mailer","title":"Mailer","text":"<pre><code>flask generate mailer &lt;MailerName&gt; [action1 action2 ...]\n</code></pre> <p>Example: <code>flask generate mailer UserMailer welcome_email</code></p>"},{"location":"other/cli/#authentication","title":"Authentication","text":"<pre><code>flask generate authentication\n</code></pre> <p>Sets up a complete authentication system (routes, controllers, views and models for managing users and sessions).</p> <p>It will add:</p> <ul> <li>A <code>User</code> model (email, password_digest).</li> <li>A <code>Session</code> model (user_id, ip_address, user_agent).</li> <li>Views and Controllers for logging in.</li> <li>Views and Controllers for editing passwords</li> <li>Views and Mailers for resetting user passwords</li> <li>Routes for session and password management</li> </ul>"},{"location":"other/fields/","title":"Fields","text":"<p>The following fields are supported in migration / model / scaffold generators:</p>"},{"location":"other/fields/#text","title":"Text","text":"<p>Use it as <code>text</code></p>"},{"location":"other/fields/#string","title":"String","text":"<p>Use it as <code>string</code> or <code>str</code></p>"},{"location":"other/fields/#integer","title":"Integer","text":"<p>Use it as <code>integer</code> or <code>int</code></p>"},{"location":"other/fields/#float","title":"Float","text":"<p>Use it as <code>float</code></p>"},{"location":"other/fields/#boolean","title":"Boolean","text":"<p>Use it as <code>boolean</code> or <code>bool</code></p>"},{"location":"other/fields/#datetime","title":"DateTime","text":"<p>Use it as <code>datetime</code></p>"},{"location":"other/fields/#date","title":"Date","text":"<p>Use it as <code>date</code></p>"},{"location":"other/fields/#time","title":"Time","text":"<p>Use it as <code>time</code></p>"},{"location":"other/fields/#json","title":"JSON","text":"<p>Use it as <code>json</code></p>"},{"location":"other/forms/","title":"Forms","text":"<p>Handling user input from forms is a common task in web applications. Flaskteroids provides a cohesive set of tools to build forms, handle submissions securely, and prevent common vulnerabilities.</p>"},{"location":"other/forms/#building-forms","title":"Building Forms","text":"<p>Flaskteroids provides a simple and powerful way to build forms using the <code>form_with</code> helper and the <code>Form</code> object. The <code>form_with</code> helper can be used with a model object to automatically generate the form's action and method, and it also includes the CSRF token automatically.</p> <p>Here's an example of how to create a form for a <code>Post</code> model:</p> <pre><code>&lt;!-- app/views/posts/_form.html --&gt;\n{% call(form) form_with(model=post) %}\n  &lt;div&gt;\n    {{ form.label('title') }}\n    {{ form.text_field('title') }}\n  &lt;/div&gt;\n\n  &lt;div&gt;\n    {{ form.label('content') }}\n    {{ form.text_area('content') }}\n  &lt;/div&gt;\n\n  &lt;div&gt;\n    {{ form.submit('Save') }}\n  &lt;/div&gt;\n{% endcall %}\n</code></pre> <p>The <code>form</code> object provides a variety of helpers for generating form fields, such as <code>text_field</code>, <code>text_area</code>, <code>password_field</code>, <code>checkbox</code>, and more.</p>"},{"location":"other/forms/#strong-parameters","title":"Strong Parameters","text":"<p>To prevent mass assignment vulnerabilities, Flaskteroids uses a technique inspired by Rails' Strong Parameters. The <code>params</code> object allows you to whitelist which parameters are permitted in your controller actions.</p> <p>This is a security best practice that ensures users cannot update model attributes they are not supposed to, such as an admin flag.</p> <p>Here's how you can use <code>params.expect</code> to safely handle incoming data. The <code>expect</code> method allows you to define the structure of the expected parameters, including nested objects and lists.</p> <pre><code># app/controllers/posts_controller.py\nfrom flaskteroids import params\nfrom flaskteroids.controller import ActionController\nfrom app.models.post import Post\n\nclass PostsController(ActionController):\n    # ...\n\n    def create(self):\n        self.post = Post.new(_post_params())\n        if self.post.save():\n            # ... success\n        else:\n            # ... error\n            pass\n\n    def update(self):\n        self.post = Post.find(params['id'])\n        if self.post.update(_post_params()):\n            # ... success\n        else:\n            # ... error\n            pass\n\n    def _post_params(self):\n        # Assumes form data is sent as:\n        # post[title]=...\n        # post[content]=...\n        return params.expect(post=['title', 'content'])\n</code></pre> <p>In this example, <code>_post_params</code> specifies that the <code>params</code> object must contain a top-level key called <code>post</code>, which should be a dictionary containing only the <code>title</code> and <code>content</code> keys. Any other attributes within the <code>post</code> object will be discarded, and if the <code>post</code> key is missing or the structure is incorrect, an exception will be raised.</p>"},{"location":"other/forms/#csrf-protection","title":"CSRF Protection","text":"<p>Flaskteroids automatically includes CSRF (Cross-Site Request Forgery) protection on all non-GET requests. A CSRF token is generated and must be included in your forms. The <code>form_with</code> helper does this automatically.</p> <p>For manual forms, you can include the token like this:</p> <pre><code>&lt;form action=\"/posts\" method=\"post\"&gt;\n  &lt;input type=\"hidden\" name=\"csrf_token\" value=\"{{ csrf_token() }}\"&gt;\n  &lt;!-- ... form fields --&gt;\n&lt;/form&gt;\n</code></pre>"},{"location":"other/jobs/","title":"Background Jobs","text":"<p>For long-running tasks, you can use background jobs. Jobs inherit from <code>Job</code> and define a <code>perform</code> method.</p> <pre><code># app/jobs/my_job.py\nfrom flaskteroids.jobs.job import Job\n\nclass MyJob(Job):\n    def perform(self, user_id):\n        # Do some long-running task\n        print(f\"Performing job for user {user_id}\")\n</code></pre> <p>To enqueue a job, call <code>perform_later</code>:</p> <pre><code>MyJob().perform_later(user_id=1)\n</code></pre>"},{"location":"other/mailers/","title":"Mailers","text":""},{"location":"other/mailers/#mailers","title":"Mailers","text":"<p>Mailers inherit from <code>ActionMailer</code> and are used to send emails from your application. They work similarly to controllers, with actions that correspond to email templates.</p> <pre><code># app/mailers/user_mailer.py\nfrom flaskteroids.mailer import ActionMailer\n\nclass UserMailer(ActionMailer):\n    def welcome_email(self, user):\n        self.user = user\n        self.mail(to=user.email, subject=\"Welcome to My App!\")\n</code></pre> <p>You can then deliver the email synchronously or asynchronously:</p> <pre><code># Deliver now\nUserMailer().welcome_email(user).deliver_now()\n\n# Deliver later using a background job\nUserMailer().welcome_email(user).deliver_later()\n</code></pre>"},{"location":"views/","title":"Views","text":"<p>Views are the user-facing part of your application. Flaskteroids uses Jinja2 for templating, which is the default for Flask. Templates are located in the <code>app/views</code> directory and are automatically rendered by controller actions.</p> <p>Instance variables set in the controller are available in the corresponding view file.</p> <pre><code>&lt;!-- app/views/posts/index.html --&gt;\n{% extends \"layouts/application.html\" %}\n\n{% block body %}\n&lt;h1&gt;Posts&lt;/h1&gt;\n\n&lt;ul&gt;\n  {% for post in posts %}\n  &lt;li&gt;{{ post.title }}&lt;/li&gt;\n  {% endfor %}\n&lt;/ul&gt;\n{% endblock %}\n</code></pre>"}]}